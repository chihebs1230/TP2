import { UserSkipTests=true } from './user--skip-tests=true.model';

describe('UserSkipTests=true', () => {
  it('should create an instance', () => {
    expect(new UserSkipTests=true()).toBeTruthy();
  });
});
